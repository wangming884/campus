services:
  app:
    image: leemingze/campus-club:latest
    container_name: campus-web-app
    restart: always
    ports:
      - "5624:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - DB_HOST=host.docker.internal
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=af16c8d83e45fa00
      - DB_NAME=campus_club
      - USE_SQLITE=false
      - PORT=3000
      - JWT_SECRET=7xK2v9Qp4mZ8aR3sT6wY1uH5dF0gJ2lB
      - NODE_ENV=production
    volumes:
      - campus_uploads_data:/app/uploads

volumes:
  campus_uploads_data:
    name: campus_uploads_data